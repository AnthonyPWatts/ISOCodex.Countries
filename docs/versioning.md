# Versioning

`ISOCodex.Countries` follows Semantic Versioning for public API changes.

Validation issue codes should be stable once published. Removing or changing canonical identifiers is breaking. Adding metadata, countries, subdivisions, aliases, or additional source details is usually non-breaking, provided existing public behaviour remains compatible.

Data updates must be documented so consumers can understand when metadata changed and why.
